class Solution {
    public int findFinalValue(int[] nums, int original) {
        int ans=original;
        
        Arrays.sort(nums);
        for(int i=0;i<nums.length;i++){
            if(ans==nums[i]){
                ans*=2;
            }

        }
        return ans;
    }
}